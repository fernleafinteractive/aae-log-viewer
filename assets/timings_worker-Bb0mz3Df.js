(function(){"use strict";onmessage=function(o){const a=o.data;if(a.length===0){postMessage({mapping:[],totalExecutionTime:0});return}const i=a.filter(t=>t.log_level==="TASK_STATUS"),e=new Map;let s=Number.MAX_VALUE,m=Number.MIN_VALUE;for(const t of i){const n=t.data.data.task_id,c=t.data.timestamp;s=Math.min(s,c),m=Math.max(m,c),e.has(n)||e.set(n,[]),e.get(n).push(t)}let g=new Map([...e.entries()].sort((t,n)=>p(n[1])-p(t[1])));const r=m-s;postMessage({mapping:g,totalExecutionTime:r}),console.log("Loaded timings data")};function p(o){const a=o.map(s=>s.data.timestamp),i=Math.min(...a);return Math.max(...a)-i}})();
